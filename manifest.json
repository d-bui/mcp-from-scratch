{
  "manifest_version": "0.3",
  "name": "users-mcp-demo",
  "display_name": "ユーザー管理デモ MCP",
  "version": "0.1.0",
  "description": "Claude との会話でユーザー管理 API を操作するデモ MCP サーバー（レイヤードアーキテクチャ発表用）。",
  "author": { "name": "d.bui" },
  "server": {
    "type": "node",
    "entry_point": "mcp/index.mjs",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/mcp/index.mjs"],
      "env": {
        "DEMO_API_URL": "${user_config.api_url}",
        "DEMO_API_KEY": "${user_config.api_key}"
      }
    }
  },
  "user_config": {
    "api_url": {
      "type": "string",
      "title": "API URL",
      "description": "ユーザー管理 API のベース URL（例: http://localhost:3000/api）",
      "default": "http://localhost:3000/api",
      "required": true
    },
    "api_key": {
      "type": "string",
      "title": "エージェント用アクセスキー",
      "description": "AI エージェント用に発行された PAT（デモの既定値: agent-demo-key）",
      "sensitive": true,
      "required": true
    }
  }
}
